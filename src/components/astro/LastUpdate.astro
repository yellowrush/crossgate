---
export interface Props {
  date?: string;
}

const { date } = Astro.props;
---

<div class="last-update">{date ? `数据更新: ${date}` : '数据更新中...'}</div>

<style>
  .last-update {
    font-size: 0.8rem;
    color: var(--text-muted);
    padding: 0.5rem 0;
  }
</style>
