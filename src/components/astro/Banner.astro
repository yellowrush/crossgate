---
export interface Props {
  img: string;
  link: string;
  alt?: string;
}

const { img, link, alt } = Astro.props;
---

<a href={link} class="banner" target="_blank" rel="noopener">
  <img src={img} alt={alt || ''} />
</a>
